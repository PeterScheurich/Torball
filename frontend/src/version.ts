/**
 * Menschenlesbare Anzeige-Version der Anwendung (erscheint in der Kopfzeile neben der Marke).
 * Bewusst als eigener Anzeigetext gehalten ("0.9 Beta"); die maschinelle semver-Version steht
 * in den package.json-Dateien der Workspaces ("0.9.0-beta"). Bei einem Versionswechsel beide
 * Stellen anpassen.
 */
export const APP_VERSION = "0.9.0 Beta";
