# Shell-Skripte MÜSSEN Unix-Zeilenenden (LF) haben - mit CRLF scheitern sie auf Linux
# ("/usr/bin/env bash\r: bad interpreter"). Erzwingt LF unabhängig von core.autocrlf des Clients.
*.sh text eol=lf
